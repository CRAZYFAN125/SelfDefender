using UnityEngine;


public class SettingsGui : MonoBehaviour
{
    /*public void GiveSignal(string k)
    {
        Settings.Keys Key = new Settings.Keys();
        if (k == "Up")
        {
            Key = Settings.Keys.Up;
        }
        if (k == "Down")
        {
            Key = Settings.Keys.Down;
        }
        if (k == "Left")
        {
            Key = Settings.Keys.Left;
        }
        if (k == "Right")
        {
            Key = Settings.Keys.Right;
        }
        if (k == "Build")
        {
            Key = Settings.Keys.Build;
        }
        if (k == "Shop")
        {
            Key = Settings.Keys.Shop;
        }
        if (k == "Destryer")
        {
            Key = Settings.Keys.Destroyer;
        }
        Settings.Instance.ApplyButton(Key);
    }*/
    
    
}